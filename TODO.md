# TODO
---
## types:
- floats // alias all primitive floating-point types. i.e: float -> f32, double -> f64.
- ints   // alias all primitive integer types. i.e: unsigned int -> u32, char -> c08.
- traits // lvalue, rvalue, remove-ref, forward, ...

## debug:
- assert        // runtime macro for terminating.
- static assert // compile-time macro for usage errors.

## containers:
- span  // fixed-size, owning c-style-array.
- array // runtime-size, owning data pointer with length/capacity.
- list  // doubly-linked list.

## memory:
-